---
title: "helm"
weight: 5
---

<!-- Code generated by cmd/gen-docs reference from schema.cue and reference.md.tmpl. DO NOT EDIT. -->

# `builtin:helm`

**Idempotent**

Installs or upgrades a Helm release on an existing Kubernetes cluster,
using the host `helm` binary.

See [Deploying workloads]({{< relref "/docs/guides/deploying-workloads" >}}).

```cue
app: {
    uses: "builtin:helm"
    needs: ["cluster"]
    with: {
        kubeconfig:   "${needs.cluster.out.kubeconfig}"
        context:      "${needs.cluster.out.context}"
        release:      "demo"
        chart:        "charts/demo"
        namespace:    "demo"
        values_files: ["values-prod.yaml"]
    }
}
```

| Field | Type | Default | Description |
|:------|:----:|:-------:|:------------|
| `kubeconfig` | `string` | - | **Required.** A path, or a `"${...}"` expression that reads one from needs, such as `"${needs.cluster.out.kubeconfig}"`. |
| `context` | `string` | - | Selects the kubeconfig context. Defaults to the kubeconfig's current-context. |
| `release` | `string` | - | **Required.** The Helm release name. |
| `namespace` | `string` | `"default"` | The target namespace. |
| `create_namespace` | `bool` | `true` | Creates namespace if it does not exist. |
| `chart` | `string` | - | **Required.** A local path (relative to the project directory unless absolute), an oci:// reference, or a chart name inside repo. |
| `repo` | `string` | - | A chart repository URL. chart then names a chart inside it. |
| `version` | `string` | - | Pins a chart version. |
| `values_files` | `[...string]` | - | Passed as repeated -f flags, in order. Relative to the project directory unless absolute. |
| `post_renderer` | `string` | - | A command that helm pipes rendered manifests through. |
| `post_renderer_args` | `[...string]` | - | Extra arguments for post_renderer. |
| `wait` | `string` | `"5m"` | How long to wait for the release to become ready, as a Go duration. Empty disables --wait. |
| `atomic` | `bool` | `true` | Rolls the release back automatically on a failed upgrade. |
| `keep` | `bool` | `false` | Leaves the release installed on Down, instead of uninstalling it. |

> [!NOTE]
> `post_renderer` is a path to the renderer executable, resolved via
> `$PATH` if it has no separator, otherwise relative to the current
> directory. This argument is different between Helm 3 and Helm 4.
> See Also: [Helm Documentation](https://helm.sh/docs/topics/advanced/)
