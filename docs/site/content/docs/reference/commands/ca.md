---
title: "ca"
weight: 8
---

<!-- Code generated by cmd/gen-docs commands from kevin's cobra command tree and reference.md.tmpl. DO NOT EDIT. -->

# `kevin ca`

Manages the kevin root CA in the trust stores of this machine. It needs no
project - the root names none and is shared by every kevin project on the
machine. See [the CA and trust guide]({{< relref "/docs/guides/ca-and-trust" >}})
for the full picture.

## `kevin ca install`

Generates the kevin root CA if it does not already exist, then adds it to
the trust stores of this machine. Run this once for the machine; there is
no need to repeat it per project.

```sh
kevin ca install
```

| Flag | Type | Default | Description |
|:-----|:----:|:-------:|:------------|
| `--firefox` | `bool` | `true` | also install into every Firefox profile's NSS database |
| `--system` | `bool` | `false` | install into the machine-wide store instead of the store of the user; needs root |

## `kevin ca uninstall`

Removes the kevin root CA from the trust stores of this machine.

```sh
kevin ca uninstall
```

| Flag | Type | Default | Description |
|:-----|:----:|:-------:|:------------|
| `--firefox` | `bool` | `true` | also remove from every Firefox profile's NSS database |
| `--system` | `bool` | `false` | remove from the machine-wide store instead of the store of the user; needs root |
