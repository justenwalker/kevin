<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Concepts on kevin</title><link>https://justenwalker.github.io/kevin/docs/concepts/</link><description>Recent content in Concepts on kevin</description><generator>Hugo</generator><language>en-us</language><atom:link href="https://justenwalker.github.io/kevin/docs/concepts/index.xml" rel="self" type="application/rss+xml"/><item><title>Architecture</title><link>https://justenwalker.github.io/kevin/docs/concepts/architecture/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/architecture/</guid><description>&lt;h1 id="architecture"&gt;Architecture&lt;a class="anchor" href="#architecture"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;A map of kevin&amp;rsquo;s parts and how they connect. Each part has its own page below with the reasoning behind its shape.&lt;/p&gt;&#10;&lt;pre class="mermaid"&gt;graph TD&#10; CLI[&amp;#34;kevin CLI&amp;#34;]&#10; ENG[engine]&#10; CFG[&amp;#34;Config&amp;#34;]&#10; DAG[&amp;#34;DAG engine&amp;#34;]&#10; HOST[pluginhost]&#10; CON[console]&#10; CA[&amp;#34;CA&amp;#34;]&#10; MCPN[MCP server]&#10; PLUG[[&amp;#34;plugin process&amp;#34;]]&#10; CRI[&amp;#34;cri&amp;lt;br/&amp;gt;(eg: docker)&amp;#34;]&#10; PROXY[proxy]&#10; LOOP([&amp;#34;host loopback&amp;#34;])&#10;&#10; subgraph DOCKERNET[&amp;#34;container network&amp;#34;]&#10; RELAY[&amp;#34;relay&amp;lt;br/&amp;gt;(container)&amp;#34;]&#10; STEPC[&amp;#34;step containers&amp;lt;br/&amp;gt;(container/kind)&amp;#34;]&#10; end&#10;&#10; CLI --&amp;gt;|invokes| ENG&#10; ENG --&amp;gt;|loads| CFG&#10; ENG --&amp;gt;|uses| DAG&#10; ENG --&amp;gt;|starts| HOST&#10; ENG --&amp;gt;|drives| CON&#10; ENG --&amp;gt;|creates| CA&#10; CON --&amp;gt;|mounts /_mcp| MCPN&#10; MCPN --&amp;gt;|tool calls, step ops| ENG&#10; HOST -.gRPC.-&amp;gt; PLUG&#10; PLUG --&amp;gt;|shells out| CRI&#10; ENG --&amp;gt;|network, GC| CRI&#10; STEPC -.-&amp;gt;|publishes| LOOP&#10; STEPC -.-&amp;gt;|resolves, forwards| RELAY&#10; RELAY -.-&amp;gt;|socks5| LOOP&#10; PROXY -.-&amp;gt;|dials| LOOP&#10; RELAY --&amp;gt;|forwards| PROXY&#10; ENG --&amp;gt;|starts| PROXY&#10; CA --&amp;gt;|signs leaf| PROXY&#10; CRI -.-&amp;gt;|creates, attaches| RELAY&#10; CRI -.-&amp;gt;|creates, attaches| STEPC&#10; RELAY -.-&amp;gt;|socks5 dial| STEPC&#10; PROXY --&amp;gt;|traffic records| ENG&#10;&#10; classDef dashedNode stroke-dasharray: 5 5&#10; class STEPC dashedNode&lt;/pre&gt;&lt;table&gt;&#10;&#9;&lt;thead&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Part&lt;/th&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Responsibility&lt;/th&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&lt;/thead&gt;&#10;&#9;&lt;tbody&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;CLI&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Parses the command line.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Engine&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Loads the environment, starts the plugins, walks the DAG.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Configuration&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Reads &lt;code&gt;kevin.cue&lt;/code&gt;. Validates every step before anything runs.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;DAG engine&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Orders the steps. Runs independent steps concurrently.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Plugin host&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Starts a plugin process and keeps the process alive.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Plugin SDK&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;The public API that a plugin author implements.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Wire contract&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;The gRPC service between the engine and a plugin.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Proxy&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Terminates TLS, routes to a workload, controls egress.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Console&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Shows the DAG state, the logs, and the proxy traffic.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;MCP server&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Exposes the running session to an MCP client over Streamable HTTP, mounted at &lt;code&gt;/_mcp&lt;/code&gt; on the console&amp;rsquo;s own listener.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;CA&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Creates the CA and mints a leaf certificate for the proxy.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Relay&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;In-network DNS + TLS/HTTP forwarder for name resolution with no host changes, plus a SOCKS5 gateway a host process can dial to reach a step directly.&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&lt;/tbody&gt;&#10;&lt;/table&gt;</description></item><item><title>Scopes and providers</title><link>https://justenwalker.github.io/kevin/docs/concepts/scopes-and-providers/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/scopes-and-providers/</guid><description>&lt;h1 id="scopes-and-providers"&gt;Scopes and providers&lt;a class="anchor" href="#scopes-and-providers"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;A project directory holds a &lt;code&gt;kevin.cue&lt;/code&gt; file. This file declares the plugins and the steps. kevin checks the file against a core schema before anything runs.&lt;/p&gt;&#10;&lt;p&gt;The environment file holds two independent DAGs, and each DAG is a scope.&lt;/p&gt;&#10;&lt;table&gt;&#10;&#9;&lt;thead&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Scope&lt;/th&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Lifetime&lt;/th&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Commands&lt;/th&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&lt;/thead&gt;&#10;&#9;&lt;tbody&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;setup&lt;/code&gt;&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Pesists across runs.&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;kevin setup&lt;/code&gt;, &lt;code&gt;kevin teardown&lt;/code&gt;&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;env&lt;/code&gt;&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Ephemeral.&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;kevin run&lt;/code&gt;&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&lt;/tbody&gt;&#10;&lt;/table&gt;&#10;&lt;p&gt;The two scopes use one engine and one protocol. An &lt;code&gt;env&lt;/code&gt; step&amp;rsquo;s &lt;code&gt;needs&lt;/code&gt; may additionally name a &lt;code&gt;setup&lt;/code&gt; step, prefixed &lt;code&gt;setup.&lt;/code&gt; (&lt;code&gt;needs: [&amp;quot;setup.&amp;lt;name&amp;gt;&amp;quot;]&lt;/code&gt;) - resolved through &lt;code&gt;Export&lt;/code&gt;, not &lt;code&gt;Up&lt;/code&gt;, since a plain &lt;code&gt;kevin run&lt;/code&gt; never brings the setup scope up in that process. See &lt;a href="https://justenwalker.github.io/kevin/docs/concepts/cross-step-values/"&gt;Cross-step values&lt;/a&gt;.&lt;/p&gt;</description></item><item><title>Cross-step values</title><link>https://justenwalker.github.io/kevin/docs/concepts/cross-step-values/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/cross-step-values/</guid><description>&lt;h1 id="cross-step-values"&gt;Cross-step values&lt;a class="anchor" href="#cross-step-values"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;Every step type speaks the same protocol over gRPC. See &lt;a href="https://justenwalker.github.io/kevin/docs/extending/plugin-protocol/"&gt;The plugin protocol&lt;/a&gt; for the RPCs and the session-startup sequence that calls them.&lt;/p&gt;&#10;&lt;p&gt;Session startup&amp;rsquo;s per-step &lt;code&gt;with&lt;/code&gt;-block validation (step 4 of that sequence) scales linearly with step count, not quadratically, on the measurements taken so far, at 1, 10, 100, and 1000 steps. Re-check that scaling before any change to how a step&amp;rsquo;s &lt;code&gt;with&lt;/code&gt; block gets validated.&lt;/p&gt;&#10;&lt;h2 id="cross-step-values-1"&gt;Cross-step values&lt;a class="anchor" href="#cross-step-values-1"&gt;#&lt;/a&gt;&lt;/h2&gt;&#10;&lt;p&gt;A step publishes outputs. Every step that declares a &lt;code&gt;needs&lt;/code&gt; edge on that step receives the outputs. A downstream step reads a value such as the endpoint of a registry, or the path of a kubeconfig file.&lt;/p&gt;</description></item><item><title>DAG engine</title><link>https://justenwalker.github.io/kevin/docs/concepts/dag-engine/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/dag-engine/</guid><description>&lt;h1 id="dag-engine"&gt;DAG engine&lt;a class="anchor" href="#dag-engine"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;The DAG engine holds a map of step names to dependency names, and validates it up front: an unknown dependency or a cycle fails before anything runs.&lt;/p&gt;&#10;&lt;p&gt;Bringing an environment up runs one goroutine per step. A goroutine waits for each of its dependencies to finish, then runs the step. The first step that fails cancels every other step still in flight.&lt;/p&gt;&#10;&lt;p&gt;A step whose dependency already failed is skipped, not failed in its own right, so the error a run reports is the root cause, not a cascade of secondary failures downstream of it.&lt;/p&gt;</description></item><item><title>Proxy</title><link>https://justenwalker.github.io/kevin/docs/concepts/proxy/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/proxy/</guid><description>&lt;h1 id="proxy"&gt;Proxy&lt;a class="anchor" href="#proxy"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;kevin changes no file on the host. There is no entry in &lt;code&gt;/etc/hosts&lt;/code&gt;, no file in &lt;code&gt;/etc/resolver&lt;/code&gt;, and no DNS server for the host.&lt;/p&gt;&#10;&lt;p&gt;A client reaches the environment through the proxy. The client sets &lt;code&gt;HTTP_PROXY&lt;/code&gt; and &lt;code&gt;HTTPS_PROXY&lt;/code&gt;, or loads &lt;code&gt;http://&amp;lt;proxy&amp;gt;/proxy.pac&lt;/code&gt;. The &lt;code&gt;CONNECT&lt;/code&gt; handler of the proxy resolves a hostname against the internal registry of the engine. A step fills the registry when &lt;code&gt;Up&lt;/code&gt; returns a &lt;code&gt;Route&lt;/code&gt;.&lt;/p&gt;&#10;&lt;p&gt;The environment has a base domain, &lt;code&gt;kevin.home&lt;/code&gt; by default. A &lt;code&gt;route&lt;/code&gt; step puts a name on it. See &lt;a href="https://justenwalker.github.io/kevin/docs/concepts/relay/#subdomain-routing"&gt;Subdomain routing&lt;/a&gt; for the general mechanism, which works the same whether the address behind the name is a container&amp;rsquo;s published port or something behind a relay. A bare step name is not a route, because a name without a dot could shadow a real host.&lt;/p&gt;</description></item><item><title>Console</title><link>https://justenwalker.github.io/kevin/docs/concepts/console/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/console/</guid><description>&lt;h1 id="console"&gt;Console&lt;a class="anchor" href="#console"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;The console renders with templ and updates with htmx over SSE. Both scripts ship embedded in the binary, thus the console needs no network.&lt;/p&gt;&#10;&lt;p&gt;The page renders the state that the server holds, then opens &lt;strong&gt;one&lt;/strong&gt; stream. Every later change arrives on that stream as a fragment that names its own target: a step row replaces itself with &lt;code&gt;hx-swap-oob&lt;/code&gt;, and a log line or a request row arrives inside an &lt;code&gt;hx-partial&lt;/code&gt; that names the region and the swap. A &lt;a href="https://justenwalker.github.io/kevin/docs/environment-file/#step-groups"&gt;step group&lt;/a&gt;&amp;rsquo;s own row is the one exception: only its header fragment swaps out of band, never the whole row, since the row also holds the checkbox that tracks whether the group reads expanded or collapsed - replacing the whole thing on every state change would silently re-collapse an already-expanded group.&lt;/p&gt;</description></item><item><title>MCP server</title><link>https://justenwalker.github.io/kevin/docs/concepts/mcp-server/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/mcp-server/</guid><description>&lt;h1 id="mcp-server"&gt;MCP server&lt;a class="anchor" href="#mcp-server"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;The MCP server gives a coding agent the same read/control surface the console gives a human, over &lt;a href="https://modelcontextprotocol.io"&gt;MCP&lt;/a&gt;&amp;rsquo;s Streamable HTTP transport instead of SSE/htmx: &lt;code&gt;list_steps&lt;/code&gt;, &lt;code&gt;get_step&lt;/code&gt;, &lt;code&gt;rerun_step&lt;/code&gt;, &lt;code&gt;export_step&lt;/code&gt;, and &lt;code&gt;get_proxy_info&lt;/code&gt;.&lt;/p&gt;&#10;&lt;p&gt;It mounts at &lt;code&gt;/_mcp&lt;/code&gt; on the console&amp;rsquo;s own HTTP router, rather than binding a listener of its own. A fourth loopback port for one more agent-facing surface would be one more thing to print, route through a firewall exception, and explain - the console already owns exactly the state an MCP tool call needs and already binds one HTTP server for exactly this session&amp;rsquo;s lifetime, so the MCP server rides on it instead of duplicating that bind/listen/shutdown dance.&lt;/p&gt;</description></item><item><title>Certificate authority</title><link>https://justenwalker.github.io/kevin/docs/concepts/ca/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/ca/</guid><description>&lt;h1 id="certificate-authority"&gt;Certificate authority&lt;a class="anchor" href="#certificate-authority"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;kevin creates the authorities and holds the private keys itself. The proxy needs a private key to sign a leaf, thus the authority does not live in a plugin. Every key is ECDSA P-256, at mode 0600 in a directory at mode 0700.&lt;/p&gt;&#10;&lt;p&gt;There are two levels.&lt;/p&gt;&#10;&lt;table&gt;&#10;&#9;&lt;thead&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Level&lt;/th&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Location&lt;/th&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Subject&lt;/th&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;th&gt;Signs&lt;/th&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&lt;/thead&gt;&#10;&#9;&lt;tbody&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Root&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;~/.kevin/&lt;/code&gt;&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;Kevin Local Root CA&lt;/code&gt;&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;the authority of a project&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&#9;&#9;&lt;tr&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;Project&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;./.kevin/&lt;/code&gt; (&lt;code&gt;./.kevin/&amp;lt;name&amp;gt;/&lt;/code&gt; for a named environment)&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;&lt;code&gt;Kevin Local Intermediate CA - Project &amp;lt;name&amp;gt;&lt;/code&gt;&lt;/td&gt;&#10;&#9;&#9;&#9;&#9;&#9;&lt;td&gt;a leaf for each host&lt;/td&gt;&#10;&#9;&#9;&#9;&lt;/tr&gt;&#10;&#9;&lt;/tbody&gt;&#10;&lt;/table&gt;&#10;&lt;p&gt;Only the root reaches a trust store, and it reaches it one time for the machine. A trust store therefore holds one kevin anchor however many projects exist. Each project signs with its own key, which lives in the project directory and goes when the directory goes.&lt;/p&gt;</description></item><item><title>Docker</title><link>https://justenwalker.github.io/kevin/docs/concepts/docker/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/docker/</guid><description>&lt;h1 id="docker"&gt;Docker&lt;a class="anchor" href="#docker"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;kevin runs the &lt;code&gt;docker&lt;/code&gt; command and parses the JSON output. kevin does not import the Docker SDK.&lt;/p&gt;&#10;&lt;p&gt;The reason is the size of the dependency. &lt;code&gt;github.com/docker/docker&lt;/code&gt; pulls in a large tree for a small number of calls. The cost of the choice is the parse of the command output, and a runtime dependency on the &lt;code&gt;docker&lt;/code&gt; binary.&lt;/p&gt;&#10;&lt;p&gt;Every container carries three labels at increasing granularity - a materialized path, each value holding every segment up to its own tier: &lt;code&gt;kevin.project&lt;/code&gt; (&lt;code&gt;&amp;quot;&amp;lt;project&amp;gt;&amp;quot;&lt;/code&gt;), &lt;code&gt;kevin.scope&lt;/code&gt; (&lt;code&gt;&amp;quot;&amp;lt;project&amp;gt;:&amp;lt;scope&amp;gt;&amp;quot;&lt;/code&gt;), and &lt;code&gt;kevin.urn&lt;/code&gt; (&lt;code&gt;&amp;quot;&amp;lt;project&amp;gt;:&amp;lt;scope&amp;gt;:&amp;lt;step&amp;gt;&amp;quot;&lt;/code&gt;). Docker&amp;rsquo;s label filter is exact-match only, with no prefix or wildcard, so each tier is its own label: &lt;code&gt;kevin.project&lt;/code&gt; finds every resource of a project in one query, &lt;code&gt;kevin.scope&lt;/code&gt; finds every resource of one scope in one query, without a &amp;ldquo;setup&amp;rdquo; step and an &amp;ldquo;env&amp;rdquo; step of the same name being confused for each other. The engine lists the containers of the project after it removes the steps, and deletes whatever is left - except a container whose &lt;code&gt;kevin.scope&lt;/code&gt; names the other scope and is still live, since setup and env share one project network. There is no state file: a label survives a crash, and a file can go stale.&lt;/p&gt;</description></item><item><title>Relay</title><link>https://justenwalker.github.io/kevin/docs/concepts/relay/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://justenwalker.github.io/kevin/docs/concepts/relay/</guid><description>&lt;h1 id="relay"&gt;Relay&lt;a class="anchor" href="#relay"&gt;#&lt;/a&gt;&lt;/h1&gt;&#10;&lt;p&gt;The relay is a container on the shared docker network. It answers DNS queries for the environment domain with its own address. It forwards HTTP and TLS traffic to the proxy on the host.&lt;/p&gt;&#10;&lt;p&gt;The relay carries no routing table and no egress policy. The proxy remains the single place that holds both. A workload that resolves a step through the relay still reaches the proxy, and the proxy routes the request by the Host header, the same as it routes every other request.&lt;/p&gt;</description></item></channel></rss>